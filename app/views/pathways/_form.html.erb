<%= simple_form_for([program, Pathway.new], data: {
    action: "submit->shortlist#update mouseenter->shortlist#mouseEnter mouseleave->shortlist#mouseLeave"
  }) do |f| %>
  <%= f.hidden_field :career_id, value: career_id %>
  <%= button_tag type: 'submit', class: "shortlist", data: { shortlist_target: 'bookmark' } do %>
      <i class="fa-regular fa-bookmark"></i>
  <% end %>


<% end %>
