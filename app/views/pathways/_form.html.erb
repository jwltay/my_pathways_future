<%= simple_form_for([program, Pathway.new], data: {
    action: "submit->shortlist#update"
  }) do |f| %>
  <%= f.hidden_field :career_id, value: career_id %>
  <%= f.submit "Shortlist",class: "shortlist"  %>
<% end %>
