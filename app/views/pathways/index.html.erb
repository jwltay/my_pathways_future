<ul>
  <% @pathways.each do |pathway| %>
    <li>
      <% if pathway.active %>
        <%= pathway.programme.name %>
        <%= link_to "Set as Goal", "#", class: 'btn btn-danger' %>
      <% end %>
    </li>
  <% end %>
</ul>
